.DS_Store
.idea/

# Go build and test output
/bin/

# `go build .` drops the executable here; the VS Code tasks build into /bin.
/onboardd
/dist/
*.test
*.out
coverage.out

# Local configuration and secrets
/config/local.toml
/config/*.local.toml
*.secret
.env

# Frontend dependencies. Production output is committed under internal/webui/dist.
node_modules/
